import { Feather } from '@expo/vector-icons';
import { router, useLocalSearchParams } from 'expo-router';
import React from 'react';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { useMockStore } from '@/context/MockStoreContext';
import { locations, products } from '@/mock/data';
import { useColors } from '@/hooks/useColors';

export default function TransferDetailsScreen() {
  const colors = useColors();
  const insets = useSafeAreaInsets();
  const { id } = useLocalSearchParams<{ id: string }>();
  const { transfers } = useMockStore();
  const record = transfers.find((item) => item.id === id);
  if (!record) return null;
  return <ScrollView style={{ flex: 1, backgroundColor: colors.background }} contentContainerStyle={{ paddingTop: Math.max(insets.top, 12), paddingHorizontal: 20, paddingBottom: 35 }}><View style={{ flexDirection: 'row', alignItems: 'center', gap: 13, marginBottom: 21 }}><Pressable onPress={() => router.back()} style={{ width: 40, height: 40, borderRadius: 13, backgroundColor: colors.card, borderWidth: 1, borderColor: colors.border, alignItems: 'center', justifyContent: 'center' }}><Feather name="arrow-left" size={18} color={colors.foreground} /></Pressable><View><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 19 }}>Transfer details</Text><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 3 }}>{record.transferNumber} · {record.status === 'POSTED' ? 'View only' : 'Draft editable'}</Text></View></View><View style={{ backgroundColor: colors.charcoal, borderRadius: 19, padding: 17, marginBottom: 17 }}><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Text style={{ color: colors.white, fontFamily: 'Inter_700Bold', fontSize: 15 }}>{record.transferNumber}</Text><Text style={{ color: record.status === 'POSTED' ? colors.success : colors.warning, fontFamily: 'Inter_700Bold', fontSize: 10 }}>{record.status}</Text></View><View style={{ flexDirection: 'row', alignItems: 'center', gap: 9, marginTop: 14 }}><Text style={{ color: colors.white, fontFamily: 'Inter_600SemiBold', fontSize: 12 }}>{locations.find((location) => location.id === record.fromLocationId)?.name}</Text><Feather name="arrow-right" size={14} color={colors.accent} /><Text style={{ color: colors.accent, fontFamily: 'Inter_600SemiBold', fontSize: 12 }}>{locations.find((location) => location.id === record.toLocationId)?.name}</Text></View><Text style={{ color: '#AEB4BA', fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 9 }}>{record.date} · {record.remarks || 'No remarks'}</Text></View><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 15, marginBottom: 11 }}>Products</Text><View style={{ backgroundColor: colors.card, borderRadius: 18, borderWidth: 1, borderColor: colors.border, overflow: 'hidden' }}>{record.lines.map((line, index) => <View key={line.id} style={{ padding: 14, borderBottomWidth: index < record.lines.length - 1 ? 1 : 0, borderBottomColor: colors.border, flexDirection: 'row', justifyContent: 'space-between' }}><View><Text style={{ color: colors.foreground, fontFamily: 'Inter_600SemiBold', fontSize: 12 }}>{products.find((product) => product.id === line.productId)?.name}</Text><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 10, marginTop: 4 }}>{line.quantity} {line.unit} · source decrease / destination increase</Text></View><Feather name="shuffle" size={16} color={colors.primary} /></View>)}</View>{record.status === 'DRAFT' && <Pressable onPress={() => router.push(`/stock/transfer-new?draftId=${record.id}` as never)} style={{ height: 52, borderRadius: 14, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center', marginTop: 17 }}><Text style={{ color: colors.white, fontFamily: 'Inter_700Bold', fontSize: 13 }}>EDIT DRAFT</Text></Pressable>}</ScrollView>;
}