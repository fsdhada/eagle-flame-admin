import { Feather } from '@expo/vector-icons';
import { router, useLocalSearchParams } from 'expo-router';
import React from 'react';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { useMockStore } from '@/context/MockStoreContext';
import { locations, products, suppliers } from '@/mock/data';
import { useColors } from '@/hooks/useColors';

export default function StockInDetailsScreen() {
  const colors = useColors();
  const insets = useSafeAreaInsets();
  const { id } = useLocalSearchParams<{ id: string }>();
  const { stockIns } = useMockStore();
  const record = stockIns.find((item) => item.id === id);
  if (!record) return null;
  const total = record.lines.reduce((sum, line) => sum + line.quantity * line.unitCost, 0);
  return <ScrollView style={{ flex: 1, backgroundColor: colors.background }} contentContainerStyle={{ paddingTop: Math.max(insets.top, 12), paddingHorizontal: 20, paddingBottom: 35 }}><View style={{ flexDirection: 'row', alignItems: 'center', gap: 13, marginBottom: 21 }}><Pressable onPress={() => router.back()} style={{ width: 40, height: 40, borderRadius: 13, backgroundColor: colors.card, borderWidth: 1, borderColor: colors.border, alignItems: 'center', justifyContent: 'center' }}><Feather name="arrow-left" size={18} color={colors.foreground} /></Pressable><View><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 19 }}>Stock In details</Text><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 3 }}>{record.stockInNumber} · {record.status === 'POSTED' ? 'View only' : 'Draft editable'}</Text></View></View><View style={{ backgroundColor: colors.charcoal, borderRadius: 19, padding: 17, marginBottom: 17 }}><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Text style={{ color: colors.white, fontFamily: 'Inter_700Bold', fontSize: 15 }}>{record.stockInNumber}</Text><Text style={{ color: record.status === 'POSTED' ? colors.success : colors.warning, fontFamily: 'Inter_700Bold', fontSize: 10 }}>{record.status}</Text></View><Text style={{ color: '#AEB4BA', fontFamily: 'Inter_500Medium', fontSize: 11, marginTop: 9 }}>{record.date} · {locations.find((location) => location.id === record.locationId)?.name}</Text><Text style={{ color: colors.accent, fontFamily: 'Inter_700Bold', fontSize: 23, marginTop: 15 }}>K {total.toLocaleString()}</Text></View><View style={{ backgroundColor: colors.card, borderRadius: 18, borderWidth: 1, borderColor: colors.border, padding: 14, marginBottom: 15 }}><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 14, marginBottom: 10 }}>Receiving details</Text><Text style={{ color: colors.foreground, fontFamily: 'Inter_600SemiBold', fontSize: 12 }}>{suppliers.find((supplier) => supplier.id === record.supplierId)?.name}</Text><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 4 }}>Invoice / reference: {record.supplierReference || '—'}</Text><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 4 }}>{record.remarks || 'No remarks'}</Text></View><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 15, marginBottom: 11 }}>Products</Text><View style={{ backgroundColor: colors.card, borderRadius: 18, borderWidth: 1, borderColor: colors.border, overflow: 'hidden' }}>{record.lines.map((line, index) => <View key={line.id} style={{ padding: 14, borderBottomWidth: index < record.lines.length - 1 ? 1 : 0, borderBottomColor: colors.border }}><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Text style={{ color: colors.foreground, fontFamily: 'Inter_600SemiBold', fontSize: 12, flex: 1 }}>{products.find((product) => product.id === line.productId)?.name}</Text><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 12 }}>K {(line.quantity * line.unitCost).toLocaleString()}</Text></View><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 10, marginTop: 4 }}>{line.quantity} × K {line.unitCost.toLocaleString()} · {line.unit}</Text></View>)}</View>{record.status === 'DRAFT' && <Pressable onPress={() => router.push(`/stock/in-new?draftId=${record.id}` as never)} style={{ height: 52, borderRadius: 14, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center', marginTop: 17 }}><Text style={{ color: colors.white, fontFamily: 'Inter_700Bold', fontSize: 13 }}>EDIT DRAFT</Text></Pressable>}</ScrollView>;
}