import { Feather } from '@expo/vector-icons';
import { router, useLocalSearchParams } from 'expo-router';
import React from 'react';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { useMockStore } from '@/context/MockStoreContext';
import { locations, products } from '@/mock/data';
import { useColors } from '@/hooks/useColors';

export default function SaleDetailsScreen() {
  const colors = useColors();
  const insets = useSafeAreaInsets();
  const { id } = useLocalSearchParams<{ id: string }>();
  const { sales } = useMockStore();
  const sale = sales.find((item) => item.id === id) ?? sales[0];
  if (!sale) return null;
  const total = sale.lines.reduce((sum, line) => sum + line.quantity * line.sellingPrice - line.discount, 0);
  return <ScrollView style={{ flex: 1, backgroundColor: colors.background }} contentContainerStyle={{ paddingTop: Math.max(insets.top, 12), paddingHorizontal: 20, paddingBottom: 35 }}><View style={{ flexDirection: 'row', alignItems: 'center', gap: 13, marginBottom: 21 }}><Pressable onPress={() => router.back()} style={{ width: 40, height: 40, borderRadius: 13, backgroundColor: colors.card, borderWidth: 1, borderColor: colors.border, alignItems: 'center', justifyContent: 'center' }}><Feather name="arrow-left" size={18} color={colors.foreground} /></Pressable><View><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 19 }}>Sale details</Text><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 3 }}>{sale.saleNumber} · {sale.status === 'POSTED' ? 'View only' : 'Draft editable'}</Text></View></View><View style={{ backgroundColor: colors.charcoal, borderRadius: 19, padding: 17, marginBottom: 17 }}><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Text style={{ color: colors.white, fontFamily: 'Inter_700Bold', fontSize: 15 }}>{sale.saleNumber}</Text><Text style={{ color: sale.status === 'POSTED' ? colors.success : colors.warning, fontFamily: 'Inter_700Bold', fontSize: 10 }}>{sale.status}</Text></View><Text style={{ color: '#AEB4BA', fontFamily: 'Inter_500Medium', fontSize: 11, marginTop: 9 }}>{sale.date} · {locations.find((location) => location.id === sale.locationId)?.name} · {sale.paymentMethod}</Text><Text style={{ color: colors.accent, fontFamily: 'Inter_700Bold', fontSize: 23, marginTop: 15 }}>K {total.toLocaleString()}</Text></View><View style={{ backgroundColor: colors.card, borderRadius: 18, borderWidth: 1, borderColor: colors.border, padding: 14, marginBottom: 15 }}><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 14, marginBottom: 12 }}>Customer</Text><Text style={{ color: colors.foreground, fontFamily: 'Inter_600SemiBold', fontSize: 12 }}>{sale.customerName || 'Walk-in Customer'}</Text>{sale.customerPhone ? <Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 4 }}>{sale.customerPhone}</Text> : null}<Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 4 }}>Payment reference: {sale.paymentReference || '—'}</Text></View><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 15, marginBottom: 11 }}>Products</Text><View style={{ backgroundColor: colors.card, borderRadius: 18, borderWidth: 1, borderColor: colors.border, overflow: 'hidden' }}>{sale.lines.map((line, index) => <View key={line.id} style={{ padding: 14, borderBottomWidth: index < sale.lines.length - 1 ? 1 : 0, borderBottomColor: colors.border }}><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Text style={{ color: colors.foreground, fontFamily: 'Inter_600SemiBold', fontSize: 12, flex: 1 }}>{products.find((product) => product.id === line.productId)?.name}</Text><Text style={{ color: colors.foreground, fontFamily: 'Inter_700Bold', fontSize: 12 }}>K {(line.quantity * line.sellingPrice - line.discount).toLocaleString()}</Text></View><Text style={{ color: colors.mutedForeground, fontFamily: 'Inter_400Regular', fontSize: 10, marginTop: 4 }}>{line.quantity} × K {line.sellingPrice.toLocaleString()} · {line.unit} · Discount K {line.discount.toLocaleString()}</Text></View>)}</View>{sale.status === 'DRAFT' && <Pressable onPress={() => router.push(`/sales/new?draftId=${sale.id}` as never)} style={{ height: 52, borderRadius: 14, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center', marginTop: 17 }}><Text style={{ color: colors.white, fontFamily: 'Inter_700Bold', fontSize: 13 }}>EDIT DRAFT</Text></Pressable>}</ScrollView>;
}